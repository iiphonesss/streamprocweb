DATABASE_URL="file:./data/card-studio.db"
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-2
CARD_STUDIO_AI_MODE=mock
CARD_STUDIO_MAX_AI_REQUESTS_PER_SESSION=20

# Local Robux Market Admin Postgres (READ-ONLY from Card Studio)
MARKET_DATABASE_URL=postgresql://postgres:postgres@localhost:5440/robux_market_admin

# Optional custom SELECT (must be SELECT-only)
# MARKET_PRODUCTS_SQL=SELECT sku, name, brand, category, "faceValue", "faceCurrency", description, images, "isActive" FROM "Product" LIMIT 5000
